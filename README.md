# Use BSV Tools

Static landing page for `https://use.bsv.tools`.

The page is designed as a builder entry point: developers choose a high-payoff BSV use case, copy an AI-agent prompt, and follow the linked starter path.

## Local Development

```bash
npm install
npm run serve
```

## Deploy

Production deploys run through CARS from GitHub Actions on pushes to `master`.
